version: '2'
syncs:
  myapp-sync:
    src: './src' # Path to your local source directory
    dest: '/app/src' # Path to the container's destination directory
    sync_host_ip: '127.0.0.1'
    sync_host_port: '10872'
    wait_for_delay: 5s
    timeout: 60
